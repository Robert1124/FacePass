# Dev-only Phase 8 legacy ONNX/Core ML conversion spike tooling.
# This file is for an isolated local Artifacts/ venv only.
# It is not an app runtime dependency and must not be added to Package.swift.
#
# coremltools 4.x is the last stable Core ML Tools line documented with the
# direct ct.converters.onnx path. The helper installs coremltools==4.1
# separately because its legacy macosx_*_intel wheel tags are not always
# discovered by modern pip on newer macOS runner images.
onnx==1.13.1
protobuf==3.20.3
numpy>=1.14.5,<1.20
